# UI Rules

- 本项目名称统一为 `codex-web`。
- UI 中绝对不允许出现内部实现说明、占位说明、后端待接说明、调试说明。
- 禁止出现类似“前端会话占位”“后端逻辑后续再接”“TODO”“mock”“placeholder”等面向开发者的文案。
- 功能未完成时，界面只能表现为自然的空状态、禁用状态或隐藏入口，不能把实现状态解释给用户看。
- 默认文案使用中文。
- 产品设计和 UI 方案不主动给建议，除非用户明确询问建议或方案取舍；用户已经给出 UI 方向时，按用户方向实现。
- 未经用户明确要求，不使用浏览器自动化或截图去做 UI 验证；UI 由用户自己体验验收。
- 如果用户明确要求使用浏览器自动化工具排障，不要盲目反复拉取页面快照或截图；优先在相关代码路径添加有边界的控制台日志/断点式日志，然后通过浏览器控制台内容对比确认状态变化。截图和快照只用于确认布局或可视结果。
- 当用户发送 Markdown 格式内容并包含本地图片链接时，默认无法直接看到图片内容；必须先读 Markdown 文本，再按 Markdown 中的真实本地路径读取图片内容，读取前不能基于图片红框、位置或截图内容做判断。
- 后端、协议适配、数据流和工程实现细节可以主动判断并落实，必要时说明边界和风险。
- 工程设计遵循“低耦合，高内聚”：协议适配、状态管理、UI 渲染、持久化和错误处理应尽量分层，避免把兼容逻辑、表现逻辑和业务状态散落在同一个组件里。
- 消息区 UI 风格要统一、克制、扁平，优先用单层结构和轻量分隔线；避免框套框、卡片套卡片、同一信息再嵌一层容器来强调。
- 当用户描述 UI 问题的边界不明确时，必须先询问具体触发条件和可观察表现；禁止猜测后修改无关内容并声称已修复。
- 修改 AI 消息/Markdown/工具调用渲染前，必须先对照 `docs/research/message-rendering-checklist.md`；不能只修截图里暴露的一小项而忽略同类元素。
- 本地完成前端/后端改动后，如需构建并重启开发服务，使用 `npm run dev:restart`。该脚本会先执行构建，随后清理本项目旧的 49380/49381 dev 进程并重启服务；不要反复重新导入同一个项目。
- 发布目录固定为 `D:\codex-web-github`；用户要求“同步到发布目录”时，默认把当前项目的差异源码同步到该目录。
